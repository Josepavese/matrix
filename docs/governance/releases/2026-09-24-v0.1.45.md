# Matrix Release Evidence: v0.1.45

## Identity and gates

- Tag `v0.1.45` resolves to `707ae079a5d7bfb8c375e3380b8069a535fedf9b` on `main`.
- Published release: https://github.com/Josepavese/matrix/releases/tag/v0.1.45
- Exact-SHA main CI: https://github.com/Josepavese/matrix/actions/runs/36005732517 — success, including Windows policy, race tests, lint, quality gate, and release dry run.
- Release workflow: https://github.com/Josepavese/matrix/actions/runs/36006137974 — success, including the exact-SHA release gate and provenance attestation.
- Local `scripts/deploy_preflight.sh`: `DEPLOY_PREFLIGHT_OK`; `scripts/quality_gate.sh --light`: `QUALITY_GATE_OK`; secret scan, governance and code governance: passed. `golangci-lint v2.13.2` was built with Go 1.27.1, matching the repository toolchain.
- `scripts/verify_release.sh v0.1.45`: `RELEASE_VERIFY_OK`, 88 checks passed, zero failed. All six archives, six SPDX SBOMs, checksums, installer assets and archive provenance were checked.

## Real behaviour

- A private, isolated Matrix/DSH home imported an existing external DSH session with an exact `session/resume` receipt and original workspace. Two prompts before release, including one after a Matrix restart, returned facts from different parts of the existing conversation while retaining the original remote session ID.
- The same isolated environment exercised worktree access after a grant, refusal after revocation, three concurrent terminal supervisor notifications, live Unix-socket delivery, idempotent retry and conflicting-key refusal, and `outcome_unknown` recovery after a forced daemon interruption without prompt replay.
- A local HTTPS ACP registry fixture served an agent index and checksum-matched artifact to `matrix install opencode`; the fixture observed no GitHub artifact request. This proves Matrix mirror selection, not an external consumer's configuration.
- Real DSH ACP model options confirmed the requested model; an invalid model failed before prompt; a separately authorized valid fallback completed and its trace recorded provider confirmation.
- After publication, the **published v0.1.45 binary** completed a prompt against the isolated imported DSH session (`run-e1c3f959-0105-4176-b55c-e6882d34e204`). Its trace retained the original remote session ID and the answer matched a fact in the session's initial instructions. A broader question about a document named “Zero to Hero” had returned uncertainty; it is not counted as semantic proof.
- None of these tests wrote the user's original DSH session: the real prompts used the private isolated copy.

## Install and runtime

- The published `install.sh` installed into an isolated `HOME` and `MATRIX_HOME`; `version`, `home`, `bootstrap doctor`, `doctor`, and `readiness` all exited successfully. The installed version reported `0.1.45` and the tagged commit.
- Before updating `/home/jose/.local/share/matrix`, the user service was stopped and the old binary, configs, and Vault were saved under `/home/jose/.local/share/matrix/backups/pre-v0.1.45-20260924-153451/`. The 791973888-byte Vault backup matched the original byte for byte (`SHA-256 817cae7c9a76ebf82e827b83d9bbd73520e8f44a8df11c884d6d98eed20896ac`).
- `scripts/deploy_local_install.sh` installed the published Linux amd64 archive after its checksum passed. Its post-install checks passed for home, Vault migration, bootstrap doctor, doctor and readiness. The user service restarted and is active; `matrix version` reports `0.1.45`, commit `707ae079`.
- An authenticated `/_matrix/runtime` request returned HTTP 200 with HTTP, A2A and JSON-RPC listeners up, Vault present, and API authentication enabled. The unauthenticated CLI readiness probe returned `ready_with_warnings` because it received HTTP 401 and fell back to local probes. The other existing warning is a missing Gemini executable. Neither is a new blocker.

## Remaining issue

`issues/2026-09-23-wall-clock-waits-in-tests.md` remains open. Its 68 test sleeps were inventoried, but the broad synchronization refactor was not completed. One separate A2A test event-ordering flake found during this release was fixed and the race suite passed. No Nido VM was created for this release; Windows CI validation and the real local Linux/DSH proofs above are the platform evidence.
