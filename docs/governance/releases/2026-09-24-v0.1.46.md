# Matrix Release Evidence: v0.1.46

## Identity and reason

- Tag `v0.1.46` resolves to `f0b97ab35e761cad6721a1ab985d3b51005f3e8e` on `main`.
- Release: https://github.com/Josepavese/matrix/releases/tag/v0.1.46
- Exact-SHA CI: https://github.com/Josepavese/matrix/actions/runs/36008200613 — success.
- Release workflow: https://github.com/Josepavese/matrix/actions/runs/36008615734 — success, including the exact-SHA gate and provenance.
- This patch follows v0.1.45 because the subsequent documentation-only CI exposed a real `runaction` proof race. When a terminal watcher recorded `late` first, the provider's later response was discarded. Matrix now appends that later proof under the same `delivery_id`; the synchronized regression test forces this order. The open wall-clock test issue records the remaining broader work.

## Verification

- `scripts/deploy_preflight.sh`: `DEPLOY_PREFLIGHT_OK`; `scripts/quality_gate.sh --light`: `QUALITY_GATE_OK`; secret scan, governance and code governance passed. The targeted regression passed 20 consecutive race-detector runs after synchronization.
- `scripts/verify_release.sh v0.1.46`: `RELEASE_VERIFY_OK`, 88 checks passed, zero failed. Checks cover the six archives, six SPDX SBOMs, checksums, installer assets, and attestations.
- Published `install.sh` installed v0.1.46 in isolated `HOME`/`MATRIX_HOME`; `version`, `home`, `bootstrap doctor`, `doctor`, and `readiness` succeeded.
- The real isolated DSH “Zero to Hero” session, worktree, supervisor socket, idempotency, interrupted-run, model, and mirror tests are recorded in [v0.1.45 evidence](2026-09-24-v0.1.45.md). The v0.1.46 production change is confined to late run-context proof; those provider paths were unchanged.

## Local upgrade

- Before installation, the running service was stopped. Its v0.1.45 binary, configs, and a fresh 791973888-byte Vault backup were saved under `/home/jose/.local/share/matrix/backups/pre-v0.1.46-20260924-155546/`. The Vault backup matched the pre-upgrade file byte for byte (`SHA-256 71c044c4afd8cf1acfb37f264b90c5e736350c70b14269aab953dda8512215df`).
- `scripts/deploy_local_install.sh` installed the published, checksum-verified Linux amd64 archive. Its post-install checks passed for home, Vault migration, bootstrap doctor, doctor, and readiness.
- The user service restarted and is active. `matrix version` reports `0.1.46`, commit `f0b97ab`; an authenticated `/_matrix/runtime` request returned HTTP 200 with Matrix HTTP, A2A and JSON-RPC listeners up, Vault present, and API authentication enabled. Gemini remains uninstalled on this host, as before.

## Scope

`issues/2026-09-23-wall-clock-waits-in-tests.md` remains open: the test suite's fixed sleeps have been inventoried, and two observed event-ordering flakes were fixed, but the broad wait refactor is unfinished. No Nido VM was created for this patch; Windows validation ran in CI.
